import axios from "axios";
import { useCallback, useEffect, useState, useMemo, useRef } from "react";
import * as S from "./styles";

const Contact: React.FC = () => {
  return <S.Contact id="contato"></S.Contact>;
};

export default Contact;
